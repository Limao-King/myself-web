# 交接文档 · 2026-09-09 · 规范铺开（初始建立）

> 本文件是**契约**，不是摘要：写"现在是什么状态、下一步做什么、什么不能碰"。
> 新会话开场：把「1–7 节」整体粘给下一个 AI，再补一句本次目标。

> ⚠️ **本文件由 DSH 于 2026-09-09 初次建立，内容从 `CHANGES.md` / `README.md` / git 状态推导，尚未经负责人逐条校正。第一次归档时必须替换为真实状态。**

---

## 1. 当前目标

维护并打磨**游戏策划求职作品集网站**（[www.limao.site](https://www.limao.site)），为秋招初筛/面试提供可展示的作品入口。
**当前状态：v2.1 设计优化已落地并 commit（`cbf4af7`）；工作树有 1 处进行中的未提交改动 —— 表情气泡定位优化。**

## 2. 已完成

| 产出 | 文件 | 状态 |
|---|---|---|
| 首轮设计优化（v2.0 → v2.1，含 3 项用户决策回退） | `CHANGES.md`、各 `src/` 文件 | ✅ 已 commit `9df9b27` |
| 审计落地 + 视觉打磨 | commit `cbf4af7` | ✅ 已 commit |
| 表情气泡定位优化（贴被点击元素外侧） | `src/layouts/Layout.astro`、`src/pages/index.astro` | ⚠️ **未提交** |
| 本工作区规范铺开（AGENTS.md + 交接文档） | `AGENTS.md`、`交接文档/` | ✅ 本次新建 |

## 3. 关键文件（下一位 AI 只需读这几个）

- `CHANGES.md` —— **改动账本 + 给后续开发者的 9 条注意事项**，改样式/布局前必读
- `交接文档/CURRENT.md` —— 本文件，当前状态契约
- `README.md` —— 项目简介（14 行）
- `AGENTS.md` —— 本工作区硬约束 + Astro 官方开发规范（已自动注入，**不要重述**）
- `src/styles/global.css` / `src/styles/eb.css` —— 主题变量表，**颜色改动只动变量**

## 4. 用户画像 / 项目背景（下一位 AI 必须知道）

- 大陆大学生，Godot 独立游戏作者，**正在秋招求职游戏策划**，初筛阶段，1–3 个月内会有笔试面试
- 这个网站的用途：**求职作品集**，展示项目经历（Minecraft RPG 地图、开放世界世界观、可玩 JRPG Demo）、策划文档、游戏经历评述
- 只用 DeepSeek 官方 API（v4.1-flash / v4-flash / v4-pro），**零订阅**
- 工具：DeepSeek Harness（DSH）Web GUI
- 技术栈：**Astro 7 + Tailwind CSS 4**，内容用 Markdown 管理；Node >= 22.12.0
- 历史坏习惯：超长会话、并行多助手跑同一项目、会话内换模型
- 偏好：喜欢先看到"为什么"再执行；**对视觉细节有明确决策权**（v2.1 曾主动回退 3 项改动）
- ⚠️ **合规红线**：在线笔试 / 面试不要用 AI

## 5. 未完成 / 下一步（按优先级）

1. **用户下一步动作**：确认本文件的推导内容；决定未提交的「表情气泡定位优化」是否 commit
2. **待内容决策**：项目卡封面 `object-fit: cover` 会裁掉童话冒险标题画面边缘 —— 需提供 16:9 封面图后替换（见 `CHANGES.md` 注意事项 9）
3. **待办**：工作树未提交改动跑一次 `npx astro build` 复验
4. **⚠️ 副本管理**：`D:\WORK\求职\个人网站搭建（改进）` 是**同一 git 仓库的旧副本**，HEAD 停在 `9df9b27`，本副本已在其之上多一个 commit，且该副本另有 7 个文件的未提交改动。**建议确认后废弃或明确其用途**，避免两边同时改造成分叉。

## 6. 坑与注意事项

- ⚠️ **颜色改动只动变量**：头部导航走 `.site-header` 变量表（`global.css`），主题差异在 `eb.css` 以变量覆盖；**不要写硬编码色、不要恢复 `!important`**
- ⚠️ **雪花屏**：`.eb-tv__play` 保持原版裸白三角（用户决策，勿再加底托/描边）；噪点压暗由 CSS `.eb-tv__static .eb-cover-warp { opacity:.55 }` 与 JS `drawStatic()` 灰阶 56–208 **两处共同承担**，缺一对比度就退化
- ⚠️ **经典视图已彻底移除**：全站不应再出现 `eb-classic` / `eb-view` / `.eb-view-toggle`
- ⚠️ **预览页**：新增设计草稿页要把路由名加进 `astro.config.mjs` 的 `PREVIEW_ROUTES`，否则会发布到生产
- ⚠️ **像素字体**：大号像素字（h1/h2 级）取 12px 整数倍（24/36/48/60/72）；小号 UI 字不受限
- ⚠️ `.eb-swap__loading` 依赖"iframe load 后 JS 置 `display:none`"的时序（`index.astro` `launch()`），重构电视区需保持
- ⚠️ 不要重述 `AGENTS.md` 的约束到本文件；`AGENTS.md` 每会话注入，必须短
- ⚠️ 交接文档必须跟着项目走，**不要建全局共享文件夹**

## 7. 验收方式

- `npx astro build` → **零报错**，25 页产物，5 个预览页路由不存在
- 视觉改动：给可复现步骤（桌面 1440px / 移动 390px，`npm run dev` → localhost:4321）
- `AGENTS.md` 在新会话里自动注入（界面出现对应 system-reminder）
- `交接文档/CURRENT.md` 只有一份，历史在 `交接文档/archive/`

## 8. 会话元信息（归档记录）

| 项 | 值 |
|---|---|
| 会话 ID | `<待第一次归档时填写>` |
| 模型 | deepseek-v4.1-flash-expires-on-0910 |
| 步数 / 平均上下文 | 本文件为初始建立，非归档产物 |
| 成本 | — |
| 归档时间 | 2026-09-09（初始建立） |
