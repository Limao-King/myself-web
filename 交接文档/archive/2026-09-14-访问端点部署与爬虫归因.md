# 交接文档 · 2026-09-14 · 访问记录端点部署 + 爬虫归因

> 归档自 `交接文档/CURRENT.md`（该文件当时记录的是 2026-09-13 安全巡检深化 + 2026-09-14 端点就绪状态）。
> 本轮新结论见同目录 `CURRENT.md`。

---

## 归档时的状态（本轮开始前）

- 本地 HEAD `ca3bcab` 比 `origin/main`（`f1a642e`）领先 5 个 commit，工作树另有 6 个文件未提交
- 待办：文档职责分离 + 访问记录端点（代码就位、**Worker 未部署**）

## 本轮（2026-09-14）完成的事

| 产出 | 文件 / 命令 | 状态 |
|---|---|---|
| ① **文档分离**：`CHANGES.md` 纯化为工程账本（482→238 行，纯删除）；运维/审计内容迁入交接文档 | `CHANGES.md`、`交接文档/2026-09-13-安全巡检与运维实测.md` | ✅ commit `8f7f3c6` |
| ② **访问记录端点上线**：Worker `POST /hit` + 主站 `sendBeacon` + R2 `analytics/days/*.jsonl` | `worker/src/index.js`、`src/layouts/Layout.astro` | ✅ commit `34f7d71`，**已部署并 push** |
| ③ **手机号暴露面处置**：删 `phone` 字段（从未被渲染）+ PDF 加 noindex | `src/site.config.ts`、`public/_headers` | ✅ commit `34f7d71` |
| ④ **爬虫归因工具**：Cloudflare GraphQL 服务器侧查询脚本 + 核验图例 | `scripts/query-cf-http-analytics.mjs`、`scripts/view-visits.mjs` | ✅ commit `61093ef` |
| ⑤ **凭据保护**：`.cf-token`/`.cf-zone`/`.cf-account` 加入 `.gitignore` | `.gitignore` | ✅ 已 `git check-ignore` 实测 |

## 本轮的三个关键结论

1. **Worker 单独部署已生效**（version `9c0990aa-1f9e-4b29-bf64-269cd95fbe0b`）；Cloudflare Pages 于 22:29:11 构建完成，线上 HTML 已含上报脚本。
2. **端到端已实测通过**：真实浏览器访问 → R2 出现记录，路径精确（`/`、`/docs/`）。
3. **服务器侧真数据推翻了旧认知**：7 天 **19,532 次请求**（不是 216），其中 94.3% 未通过 Cloudflare 验证；`/robots.txt`、`/sitemap.xml`、`/cdn-cgi/rum` 是主要目标。详见新 `CURRENT.md`。

---

（以下为归档前 `CURRENT.md` 的正文，原文保留）

---

# 交接文档 · 2026-09-13 · 安全巡检深化（Worker 加固 + 全站暴露面实测）

> 本文件是**契约**，不是摘要：写"现在是什么状态、下一步做什么、什么不能碰"。
> 新会话开场：把「1–7 节」整体粘给下一个 AI，再补一句本次目标。

---

## 1. 当前目标

维护并打磨**游戏策划求职作品集网站**（[www.limao.site](https://www.limao.site)），为秋招初筛/面试提供可展示的作品入口。
**当前状态：线上已生效（Worker 已 `wrangler deploy` 并复验通过）；本地 HEAD `ca3bcab` 比 `origin/main`（`f1a642e`）领先 5 个 commit，且工作树另有 6 个文件的未提交改动（2026-09-14 的两件事：文档职责分离 + 访问记录端点）。用户选择"只 commit 不 push"，尚未推送。**
**⚠️ 访问记录端点（`POST /hit`）目前只在本地：Worker 尚未重新部署 → 主站的新上报脚本在线上会被 200 响应，但 `/hit` 端点不存在、不会记录。部署是下一步第一件事。**

## 2. 已完成（本轮 5 件事）

| 产出 | 文件 / 命令 | 状态 |
|---|---|---|
| ① **Worker 加固**：方法门（非 GET/HEAD → 405 + `Allow`）、畸形编码 500→400、R2 异常→502、补 4 个安全头（**错误响应也带全套**）、HEAD 空体 | `worker/src/index.js` | ✅ commit `4507831`，**已部署生效** |
| ② **关闭 workers.dev 与版本预览 URL**（本次部署曾把两者默认打开） | `worker/wrangler.toml`：`workers_dev = false` + `preview_urls = false` | ✅ commit `4507831`，已部署，API 查证 `enabled:false` |
| ③ **安全巡检与运维实测全量记录**（v2.4 六项复验表 / 改动表 / 46 项离线断言 / 部署后线上复验 / 未处理项 / 1 条被推翻的误判） | `交接文档/2026-09-13-安全巡检与运维实测.md` | ✅ 已搬迁 |
| ④ 账号侧三项核实：GitHub 2FA ✅ + Cloudflare 2FA ✅、**R2 公开访问实测已关闭** | 同上文件 | ✅ commit `a2ed503` |
| ⑤ 部署与实测复验 | `cd worker && npx wrangler deploy` | ✅ 线上生效版本 `9912d9a7-1298-4925-b511-1e85e18a75d3` |

## 2b. 已完成（2026-09-14 这一轮 · 未提交）

| 产出 | 文件 | 状态 |
|---|---|---|
| ⑥ **流量异常定性**（156→216 次浏览量的自动化占比、与安全维护无因果） | `交接文档/2026-09-13-安全巡检与运维实测.md` | ✅ 已改 |
| ⑦ **文档职责分离**：`CHANGES.md` 纯化（482→235 行、纯删除） | `CHANGES.md`、交接文档 | ✅ 已改 |
| ⑧ **访问记录端点（只写不读）**：**32/32 离线断言通过** | `worker/src/index.js`、`src/layouts/Layout.astro` | ⚠️ 代码就位、构建通过，**Worker 未部署** |
| ⑨ **访问数据查看工具** | `scripts/view-visits.mjs` | ✅ 可用 |
| ⑩ **手机号暴露面处置** | `src/site.config.ts`、`public/_headers` | ✅ 已改 |

## 3. 关键文件

- `CHANGES.md` —— 工程改动账本 + 开发者注意事项，**改样式/布局前必读**。**只写"改了代码什么"**；运维/安全/账号面内容写进 `交接文档/`。⚠️ **公开仓库，按公开标准写**
- `交接文档/2026-09-13-安全巡检与运维实测.md` —— 安全巡检 + 部署运维 + 账号面实测 + 相关坑
- `scripts/view-visits.mjs` —— 查看主站访问记录；只依赖 `wrangler r2 object get`
- `scripts/test-hit-endpoint.mjs` —— `/hit` 端点离线断言（32 项）
- `worker/src/index.js` —— Worker 源码；`FRAME_ANCESTORS` 白名单在这里
- `worker/wrangler.toml` —— **`workers_dev` / `preview_urls` 必须保持 `false`**
- `src/styles/global.css` / `src/styles/eb.css` —— 主题变量表，**颜色改动只动变量**
- `src/layouts/Layout.astro` —— 里程计 / 数字滚轮 / 表情气泡 / reveal 动画（threshold 必须为 0）
- `public/_headers` —— Pages 安全头（**故意不含通用 CSP 与 HSTS**）

## 4. 用户画像 / 项目背景

- 大陆大学生，Godot 独立游戏作者，**正在秋招求职游戏策划**（2027 届），初筛阶段
- 网站用途：**求职作品集**（项目经历 / 策划文档 / 游戏经历评述）
- 只用 DeepSeek 官方 API，**零订阅**；工具是 DSH Web GUI
- 技术栈：**Astro 7 + Tailwind CSS 4**，内容用 Markdown 管理；Node >= 22.12.0
- **两条独立部署链**：主站 = Cloudflare Pages 绑 GitHub（`push` 后 2–3 分钟自动构建）；`play.limao.site` = 独立 Worker `fairytale-game` + R2 `myself-web-game`（**必须 `wrangler deploy`，push 不更新它**）
- 账号：Cloudflare = `limao233666@outlook.com`
- 偏好：先看"为什么"再执行；**视觉细节有决策权**；喜欢**实测**而非推测
- ⚠️ **合规红线**：在线笔试 / 面试不要用 AI
- ⚠️ **本机网络状况**：`*.workers.dev` 是 DNS 黑洞；`git ls-remote` 可能 `Connection was reset`

## 5. 未完成 / 下一步

1. **部署访问记录端点**：`cd worker && npx wrangler deploy`（push 不会更新它）
2. **决定是否 `git push`**；建议先部署 Worker，并把文档分离与端点实现拆成不同 commit
3. **（顺延）`Always Use HTTPS` 已解决**，不要再提。**仍未处理（低优先级）**：裸域 `limao.site` 无任何 DNS 记录
4. **⚠️ Pages 历史部署 URL 残留**：117 个旧部署已删除（134→17），但抽样发现 2 个已删 URL（`92e106ed` / `c5e2790d`）仍在边缘执行旧 Function → 需给 `*.myself-web-3w8.pages.dev` 加 **Cloudflare Access** 或找支持
5. **用户已明确"暂不处理"，勿反复提**：HSTS、`public/_routes.json` 清理、简历 PDF 元数据
6. **待内容决策**：项目卡封面 `object-fit: cover` 裁边 → 需 16:9 封面图
7. **已问过且选择"先不做"**：`robots.txt` / `sitemap.xml`、`.gitignore` 中文注释乱码、首页 2 处 `alt=""`
8. **背景信息（勿重复排查）**：Web Analytics 的境外访问已定性为爬虫/审计类自动化流量，**与安全维护无因果**；本地取证该时段无 DSH 会话

### 副本说明（**不是风险，勿再提议废弃**）

- `D:\WORK\求职\个人网站搭建（改进）` = 交给**其他 AI 做改造的实验副本**。**主仓库是唯一发布源。**

## 6. 坑与注意事项

- ⚠️ **文档职责分离**：`CHANGES.md` = 只记工程改动；`交接文档/` = 状态/安全/运维/账号面
- ⚠️ **Worker 部署是独立一步**；需要本机 wrangler 凭证；OAuth 报 `CSRF value does not match` 时重开一次即可
- ⚠️ **`wrangler.toml` 不显式写 `workers_dev` 时默认值会变**——已写死 `false`，别删
- ⚠️ **Worker 的 `frame-ancestors` 必须含 `https://www.limao.site`，绝不能是 `'none'`**
- ⚠️ **`decodeURIComponent(url.pathname)` 必须包 `try/catch`**（畸形 `%FF` → 1101 500）
- ⚠️ **绝对不要给主站加 COEP**（会拦掉跨域 iframe，试玩打不开）
- ⚠️ **R2 Public access 关闭状态下界面只有「Allow Access」按钮**，确认状态用 API
- ⚠️ **`functions/` = 生产公开端点**；**Pages 每次构建都留永久公开 URL，且"删除部署"≠立即下线**
- ⚠️ **reveal 的 `threshold` 必须是 0**（否则 docs 详情页永久 `opacity: 0`）
- ⚠️ **`.eb-odo` 的 `vertical-align` 是语境相关的**；改字号后要重新像素级校准
- ⚠️ **`.eb-emoji-pop` 定位必须传第 4 个参数**（被点击元素）
- ⚠️ **颜色改动只动变量**，不要硬编码色
- ⚠️ **雪花屏**：`.eb-tv__play` 保持裸白三角；噪点压暗由 CSS + JS 两处共同承担
- ⚠️ **预览页**：新增草稿页要把路由名加进 `astro.config.mjs` 的 `PREVIEW_ROUTES`
- ⚠️ **移动端 `pre` 不要改成 `overflow: hidden`**
- ⚠️ **像素字体**：大号像素字取 12px 整数倍
- ⚠️ **`求职素材集/` 从未进过 git 历史**——别加进版本控制
- ⚠️ 交接文档跟着项目走，**不要建全局共享文件夹**

## 7. 验收方式

- `npx astro build` → 零报错，26 页产物
- `curl -X POST https://play.limao.site/` → **405**；`/%FF` → **400**
- 首页点「开始试玩」→ iframe 内 Godot 画面正常渲染
- 主站 `/games/<随机串>` 应返回 **404**
- `/docs/world-regions/` 源码应含 `threshold: 0`
- **访问记录端点**：`node scripts/test-hit-endpoint.mjs` → 32 项全通过；浏览器打开主站等 2 秒 → `node scripts/view-visits.mjs --days 1` 出现记录
- `grep -n "wrangler" CHANGES.md` 应零命中；文档中不得出现本机绝对路径/用户名/凭证目录

## 8. 会话元信息（归档记录）

| 项 | 值 |
|---|---|
| 会话 ID | `本次会话（用户可在 DSH GUI 里查看）` |
| 模型 | deepseek-v4.1-flash-expires-on-0910 |
| 步数 / 平均上下文 | 约 60 步；上下文已超 150K（中途做过一次压缩） |
| 成本 | 未统计 |
| 归档时间 | 2026-09-13 21:35（2026-09-14 补记） |
